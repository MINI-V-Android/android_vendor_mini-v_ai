cc_binary {
    name: "ai_daemon",
    defaults: ["miniv_ai_defaults"],

    srcs: [
        "main.cpp",
        "src/llm_engine.cpp",
    ],

    local_include_dirs: ["include"],

    static_libs: [
        "libllama-prebuilt",
        "libggml-prebuilt",
        "libggml-base-prebuilt",
        "libggml-cpu-prebuilt",
    ],

    init_rc: [
        "etc/init.mini-v-ai.rc",
    ],
}
